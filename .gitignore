# dependencies / build output
node_modules/
dist/

# credentials & keys — never commit
secrets/
fdms-test-credentials.md
*.pem
*.p12
*.pfx
.env
.env.*

# personal / business files — never publish
Goko Consultancy Tax  clearance.pdf
ZIMRA CHAT FABLE.txt

# README images — live on the orphan `assets` branch, not in main
assets/

# editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db

# logs & coverage
*.log
coverage/

# CLI device profiles (contain private keys)
.zimra/
